page-layout: article
date-format: "MMMM D, YYYY"   # weekly session date shown in the .date-banner

# Use the date-banner title-metadata partial on all content pages
format:
  html:
    template-partials:
      - ../html/title-metadata.html
